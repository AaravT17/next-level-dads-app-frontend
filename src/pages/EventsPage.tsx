import { ScopedCollectionPage } from './collections/ScopedCollectionPage'

/** Events, promoted out of the Groups tab bar into their own destination. */
const EventsPage = () => <ScopedCollectionPage kind="events" title="Events" />

export default EventsPage
